import React from 'react';

import Layout from '../components/LayoutComponent';

const NotFoundPage = () => (
    <Layout
        hasFooter={false}
        hasHeader={false}
    >
        <h1>NOT FOUND</h1>
    </Layout>
);

export default NotFoundPage;
